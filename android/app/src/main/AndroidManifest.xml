<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- "Ekran merceği" (screen lens) özelliği: diğer uygulamaların üzerine
         sürüklenebilir bir büyüteç çizip, bırakılan noktadaki metni OCR ile
         okur. SYSTEM_ALERT_WINDOW overlay çizimi için, FOREGROUND_SERVICE* ve
         POST_NOTIFICATIONS ise ekran yakalama oturumunu arka planda canlı
         tutan servis için gerekli. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="Çince Sözlük"
        android:name=".ZhEnDictApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".WidgetConfigureActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>

        <!-- Opened by tapping a widget card: a transparent, floating
             "popup" Activity, not the main app UI. excludeFromRecents +
             taskAffinity="" keep it from showing as its own entry in the
             recent-apps list. -->
        <activity
            android:name=".WordPopupActivity"
            android:exported="false"
            android:theme="@style/PopupTheme"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:windowSoftInputMode="adjustResize" />

        <receiver
            android:name=".WordWidgetProvider"
            android:label="Kelime / Gramer"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/word_widget_info"/>
        </receiver>

        <!-- ScreenUnlockReceiver is NOT declared here: ACTION_USER_PRESENT
             is only ever delivered to dynamically-registered receivers (see
             ZhEnDictApplication), so a manifest entry for it would just be
             dead weight — silently never invoked by the OS. -->

        <!-- Handles the widget's tap actions (next word / favorite). -->
        <receiver
            android:name=".WidgetActionReceiver"
            android:exported="false"/>

        <!-- Invisible relay: requests the overlay + screen-capture
             permissions (if not already granted) then starts/stops
             OcrOverlayService and finishes itself. Never actually shows any
             UI of its own. -->
        <activity
            android:name=".ScreenCapturePermissionActivity"
            android:exported="false"
            android:theme="@style/PopupTheme"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- The screen-lens overlay: a foreground service so it can keep the
             MediaProjection screen-capture session alive while the user is
             in another app. mediaProjection type is required on Android 14+
             for services that use MediaProjection. -->
        <service
            android:name=".OcrOverlayService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />


        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility: without declaring interest in
             TTS engines, flutter_tts can't see or bind the system
             text-to-speech service and every speak() call fails silently
             (no sound, no error) on real devices. -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
    </queries>
</manifest>
