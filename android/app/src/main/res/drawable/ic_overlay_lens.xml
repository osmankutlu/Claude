<!-- The draggable lens cursor: an "L" corner mark, like a crop/select-tool
     handle. Its sharp bottom-left corner is the precise hotspot — the exact
     point OCR samples around — matching how the code positions it (see
     OcrOverlayService.showLens's ACTION_UP handler). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="34dp" android:height="34dp"
    android:viewportWidth="24" android:viewportHeight="24">
    <path
        android:fillColor="@color/widget_accent"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="1"
        android:pathData="M2,2 L6,2 L6,18 L22,18 L22,22 L2,22 Z"/>
</vector>
